# Supabase Configuration
# Get these values from your Supabase project: Settings → API

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key-here

# Server-side Supabase URL alias (used by all /api/ serverless functions)
# Same value as VITE_SUPABASE_URL — needed because Vercel serverless functions
# use process.env (not import.meta.env)
SUPABASE_URL=https://your-project-ref.supabase.co

# Service Role Key (server-side only — bypasses RLS, NEVER expose to frontend)
# Get from: Supabase Dashboard → Settings → API → service_role key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================================================
# BILLING (Coming Soon)
# ============================================================================

# Stripe Payment Processing
# Get these from: https://dashboard.stripe.com/apikeys
# Publishable key (safe to expose in frontend)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key-here

# Secret key (NEVER expose to frontend - backend/serverless only)
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key-here

# Stripe Webhook Secret (for verifying webhook signatures)
# Get this from: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret-here

# ============================================================================
# GOOGLE CALENDAR INTEGRATION
# ============================================================================

# Google OAuth 2.0 Credentials
# Get these from: https://console.cloud.google.com/apis/credentials
# 1. Create a new project or select existing
# 2. Enable Google Calendar API
# 3. Create OAuth 2.0 Client ID (Web application type)
# 4. Add authorized redirect URI: http://localhost:4321/auth/google/callback (or your domain)

# Client ID (safe to expose in frontend)
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Client ID for server-side usage (same value as VITE_GOOGLE_CLIENT_ID)
# Required by /api/google-calendar/exchange-token
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Client Secret (NEVER expose to frontend - backend/serverless only)
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# Redirect URI (must match what's configured in Google Console)
# For local development:
GOOGLE_REDIRECT_URI=http://localhost:4321/auth/google/callback
# For production, update to your domain:
# GOOGLE_REDIRECT_URI=https://yourdomain.com/auth/google/callback

# Client-side redirect URI (optional — falls back to window.location.origin)
# VITE_GOOGLE_REDIRECT_URI=http://localhost:4321/auth/google/callback

# ============================================================================
# SOLANA / WEB3 — Proof of Handshake
# ============================================================================

# Network: 'devnet' or 'mainnet-beta'
VITE_SOLANA_NETWORK=devnet

# RPC URL (optional — defaults to public cluster URL)
# For production, use a dedicated RPC from Helius, QuickNode, or Triton
# VITE_SOLANA_RPC_URL=https://rpc.helius.xyz/?api-key=your-key

# Treasury wallet (receives 0.01 SOL handshake fees)
# VITE_TREASURY_WALLET=your-treasury-public-key

# Merkle tree authority keypair (base58 secret key — server-side only)
# Used by the minting endpoint to create soulbound cNFTs
# HANDSHAKE_TREE_KEYPAIR=your-base58-secret-key

# Merkle tree address (public key — set after running setup-merkle-tree script)
# HANDSHAKE_MERKLE_TREE=your-merkle-tree-public-key

# ============================================================================
# X (TWITTER) OAUTH — Trust Score Verification
# ============================================================================

# X OAuth 2.0 Credentials
# Get these from: https://developer.x.com/en/portal/dashboard
# 1. Create a project and app
# 2. Under "User authentication settings", enable OAuth 2.0
# 3. Set redirect URL to: https://app.convenu.xyz/api/auth/x (or your domain)
# 4. Scopes required: tweet.read, users.read

# X_CLIENT_ID=your-x-client-id
# X_CLIENT_SECRET=your-x-client-secret
# X_CALLBACK_URL=https://app.convenu.xyz/api/auth/x

# ============================================================================
# TELEGRAM BOT
# ============================================================================

# Telegram Bot Token (server-side only)
# Get from: @BotFather on Telegram
# TELEGRAM_BOT_TOKEN=your-telegram-bot-token

# Web app URL (used for Telegram bot claim links and CORS)
# WEBAPP_URL=https://app.convenu.xyz

# ============================================================================
# OPTIONAL: AUDIO TRANSCRIPTION
# ============================================================================

# OpenAI API (for Whisper audio transcription)
# Get your API key from: https://platform.openai.com/api-keys
# Only needed if implementing audio input feature
# OPENAI_API_KEY=sk-your-openai-api-key-here
